# Minimal production requirements for the FastAPI inference service.
# The full requirements.txt (with jupyter, mlflow, etc.) is only needed
# for local development and training.

# Core data
pandas>=2.2.0
numpy>=1.26.0

# ML
scikit-learn>=1.4.0
xgboost>=2.0.3
joblib>=1.3.0

# API
fastapi>=0.109.0
uvicorn[standard]>=0.25.0
pydantic>=2.5.3

# Config & utils
pyyaml>=6.0.1
python-dotenv>=1.0.0
python-json-logger>=2.0.7
